<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sean.takeastand"
    android:versionCode="10"
    android:versionName="A10">
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>


    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppThemeWithoutActionBarIcon">

        <activity
            android:name=".ui.MainActivity"
            android:label="@string/app_name"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.ScheduleListActivity"/>
        <activity android:name=".ui.ScienceActivity" />
        <receiver android:name=".alarmprocess.AlarmReceiver" ></receiver>
        <receiver android:name=".alarmprocess.StartScheduleReceiver" ></receiver>
        <receiver android:name=".alarmprocess.BootReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
            </intent-filter>
        </receiver>
        <service android:name=".alarmprocess.AlarmService"></service>
        <service android:name="com.heckbot.standdetector.StandDtectorTM" />
        <receiver android:name="com.heckbot.standdetector.MyBroadcastReceiver" />
    </application>

</manifest>
